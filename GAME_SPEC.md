# 《第九机位》首个可玩竖切片

## 玩家幻想

玩家扮演婚礼纪录片摄影师路野。序幕中，玩家亲自拍下苏晚隔门示警与全酒店停电；之后通过匿名回放地址进入八台封存机位。真正的压力来自“我明明亲眼见过她，但回放里的八个现在并不一致”。

## 本阶段范围

- 可玩时间窗：`23:46:55:00`—`23:47:05:00`，24 fps。
- 九个机位可切换；CAM-09 初始锁定，发现四条核心矛盾、建立两条有效联结、完成四帧时间剪辑并做出正确推断后开放。
- 每个机位根据播放时间自动选择事件前 / 事件后视觉状态。
- 玩家可播放、暂停、拖拽、逐帧、使用数字键切换机位，并按 `E` 开启自由检视。
- 发现四条核心证据后，系统不直接公布答案，只暴露“时间基准不一致”。

## 核心循环

1. 身份序幕明确“我是谁、我在哪、我与苏晚的关系”，再让玩家完成身份立场、现场行动、四次画面跟焦和现场记忆选择；失焦表现会影响隐藏结局。
2. 玩家自由切换八台机位、播放或拖动十秒；默认不提供目标机位、目标秒数或证据框。停滞十秒后，“时间回声”给出机位与大致秒数，但不揭示热点。
3. 异常强度只反馈是否靠近时间错位；玩家开启可移动放大镜，在局部放大画面中亲自指出可疑细节。
4. 正确观察会打开物证信息框，展示 GPT Image 2 物证近照、时间与说明；收录后进入证据抽屉。玩家把两条带图记录放入联结台，以双图对照验证物件、路径或信号是否彼此矛盾。
5. 四条核心证据与两条有效联结齐备后，玩家先把黑卡、露台副本、镜内门缝与设备箱残印按真实顺序剪回母带，再判断“八台机位不处在同一个现在”。
6. 玩家处理第九机位，进入三个基础结局之一；完整档案、低误判或序幕中的身份与跟焦表现可开放“两个婚礼”“归还姓名”两个隐藏结局。

## 证据集合

| ID | 机位 | 时间 | 画面证据 | 权重 |
|---|---|---:|---|---|
| `mirror-door` | 04 | 23:47:01 | 镜内门与直接空间不一致 | 核心 |
| `missing-case` | 05 | 23:47:02 | 黑箱消失，只剩干燥矩形印 | 核心 |
| `footprint-gap` | 06 | 23:47:01 | 五枚脚印与门口之间存在干燥断层 | 补充 |
| `duplicate-card` | 07 | 23:47:00 | 黑卡出现在露台排水沟旁 | 核心 |
| `card-in-hand` | 08 | 23:46:59 | 同一黑卡仍在苏晚手中 | 核心 |

## 证据联结

| 联结 | 证据组合 | 解封档案 |
|---|---|---|
| 一物双存 | 苏晚手中的黑卡 + 露台黑卡 | B-09 / 见证人凭证 |
| 路径被剪断 | 镜内门缝 + 脚印断层 | FLOOR 0 / 西翼夹层 |
| 被搬走的第九路信号 | 干燥矩形印 + 露台黑卡 | SYNC-09 / 未登记输入 |

完成任意两条有效联结即可进入时间重建，联结台会明确显示“主线已就绪”。第三条作为主动探索奖励放在档案列末位并标记为“可选调查”；若缺少证物，可从档案卡直接追踪对应机位与秒数并自动开启放大镜。它不阻断主线，但零误判且解封三页档案才可获得 S 评级。时间重建连续失败两次会显示母带时间码辅助，不会卡死；完成后，已建立联结会先以双图矛盾回放带重新呈现，再开放结论选择。

## 输入动作表

| 动作 | 键鼠 | 触屏 |
|---|---|---|
| 播放 / 暂停 | `Space` | 播放按钮 |
| 逐帧后退 / 前进 | `←` / `→` | 两侧逐帧按钮 |
| 切换机位 | `1`—`9` | 机位编号 |
| 开启 / 退出检视 | `E` / `Esc` | 检视按钮 |
| 指出可疑细节 | 移动鼠标并点击；方向键移动准星、回车确认 | 点击画面 |
| 证据抽屉 | `C` | 证据按钮 |
| 证据联结台 | `B` | 抽屉内联结按钮 |
| 调取方向提示 | 顶栏“提示” | 顶栏“提示” |
| 重置片段 | `R` | 抽屉内重置 |

## 架构边界

- XState 拥有可序列化的播放时间、机位、播放状态、证据集合、误判次数、推断结果和抽屉状态。
- PixiJS 只负责当前机位图片、交叉淡变、扫描光与轻微镜头呼吸，不拥有游戏规则。
- React DOM 负责 HUD、时间轴、按钮、证据热点、键盘焦点与响应式布局。
- 首次进入由状态机锁定在 `intro`；五段序幕完成后才开放回放输入，右上角“规则”只展示行动规则。
- Web Audio 在首次点击后生成中低频铺底、可听域脉冲和分场景音符；序幕、调查、异常接近和揭示使用不同动态音色，无外部音轨或版权素材。UI 直接读取 AudioContext 状态，不能用“开启”掩盖浏览器实际暂停。
- 角色对白与旁白仅保留字幕，运行时不创建 HTML Audio、不请求语音文件，也不朗读字幕。
- 十秒辅助计时由 React 管理；只有证据、联结或推断产生进展时才重置，提示跳转不会替玩家开启检视或记录证据。
- 资产只通过稳定 ID 暴露给运行时；文件名不作为玩法规则。
- 本阶段不写入永久存档。刷新即重置，调试时可从 URL 参数恢复测试状态。

## 失败与重置

- 没有传统死亡；错误观察与错误推断会降低 CAM-09 的观察评级。
- 到达 23:47:05 自动暂停，允许回看。
- `R` 重置时间、机位和证据，但不触碰外部文件或项目资产。

## 性能与可访问性

- 首屏先加载轻量序幕，PixiJS 画面引擎拆分为延迟代码块；进入后预取其他机位。
- 固定 21:9 舞台，DOM 热点按百分比映射，避免分辨率耦合。
- `prefers-reduced-motion` 下关闭扫描光、呼吸与交叉移动，只保留瞬时状态反馈。
- 所有按钮可键盘聚焦；检视镜支持方向键与回车；颜色不是唯一状态标识。
- 常态界面没有目标机位红点、目标时间刻度或证据框，只保留案件问题、异常强度和已发现证据刻度。
